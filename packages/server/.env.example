# Port for the Colyseus server
PORT=2567

# Firebase service account — base64 encoded JSON from Google Cloud Console
# cat service-account.json | base64
FIREBASE_SERVICE_ACCOUNT=

# Or leave blank and set GOOGLE_APPLICATION_CREDENTIALS for GCP auto-credentials
GOOGLE_APPLICATION_CREDENTIALS=
